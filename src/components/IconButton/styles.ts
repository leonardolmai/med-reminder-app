import styled from "styled-components/native";
import { TouchableOpacityProps } from "react-native";

export const IconButtonContainer = styled.TouchableOpacity<TouchableOpacityProps>`
  align-items: center;
  justify-content: center;
`;

